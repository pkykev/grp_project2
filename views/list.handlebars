
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Listing</title>
    <script src="https://cdn.jsdelivr.net/npm/handlebars@latest/dist/handlebars.js"></script>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-4">
    <h1>Find People</h1>
    <div id="user-list"></div>
  </div>

  <script id="user-template" type="text/x-handlebars-template">
    {{#each users}}
    <div class="card mt-3">
      <div class="card-body">
        <h5 class="card-title">{{first_name}} {{last_name}}</h5>
        <p class="card-text"><strong>Email:</strong> {{email}}</p>
        <p class="card-text"><strong>Gender:</strong> {{gender}}</p>
        <p class="card-text"><strong>Sexual Orientation:</strong> {{sexual_orientation}}</p>
        <p class="card-text"><strong>Primary Language:</strong> {{primary_language_spoken}}</p>
        <p class="card-text"><strong>Cruise Location:</strong> {{cruise_location}}</p>
      </div>
    </div>
    {{/each}}
  </script>

  <script src="/js/list.js"></script>
</body>
</html>