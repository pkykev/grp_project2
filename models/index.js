// this file is used to creat table relations must require in and export out all models used in this file
const User = require('./User');
const Mail = require('./Mail')

module.exports = { User, Mail };
